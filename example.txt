How the tic-tac-toe program will run

played = false
$play = true

while $play 
	puts played? "Ready?! Player 1(X), Player 2(O)?" : "Again!? #Depending on how many times they've played"
	>input
	
	board = Board.new
	players = []
	player << Player.new
	player << Player.new
	
		until victory is acheived
			board.display #Show the grid/board
			
			
			Player 1(X), select position:
			>input
			
			(add the input to the board)
			
			board.display #show the grid/board

			Player 2(O), Select position: 
			>input

			(add the input to the board)

			(Display)
			...
		end
		
	(call out the victor)
		
	(change the score on the top left.)
end