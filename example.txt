How the tic-tac-toe program will run

play = true
while play 
	Ready?! Player 1(X), Player 2(O)? || Again!? #Depending on how many times they've played
	>input
	(Create a new instance of the board and players if none already exist)


	(Display Board, GRID A-C,1-3; score top-left edge)

		until victory is acheived
		
			Player 1(X), select position:
			>input

			(Refresh The Board with new input, check for victory)

			(Display Board)

			Player 2(O), Select position: 
			>input

			(Refresh)

			(Display)
			...
		end
		
		(call out the victor)
		
		(change the score on the top left.)
		
		
	end
end